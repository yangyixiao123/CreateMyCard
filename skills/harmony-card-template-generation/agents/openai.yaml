interface:
  display_name: "Harmony 卡片模板生成"
  short_description: "基于固定布局和受控组件变体生成 HarmonyOS 服务卡片。"
  default_prompt: "使用 $harmony-card-template-generation 根据用户需求选择模板并生成最终 genui 和 cardspec。"

policy:
  allow_implicit_invocation: false
