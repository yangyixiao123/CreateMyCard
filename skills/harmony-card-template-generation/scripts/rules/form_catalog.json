{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ohos.a2ui.extended.catalog",
  "title": "HarmonyOS A2UI Form Catalog",
  "description": "Complete catalog for HarmonyOS A2UI Form (service card) protocol extensions. Form only supports extended components, not A2UI native components.",
  "catalogId": "ohos.a2ui.extended.catalog",
  "expressionGrammar": "expression_grammar.ebnf",
  "expressionParserGrammar": "expression_grammar.lark",
  "$defs": {
    "ExtendedComponentCommon": {
      "type": "object",
      "description": "Common properties for all extended components",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique component identifier within the surface"
        },
        "component": {
          "type": "string",
          "description": "Component type name (e.g. Text, Button)"
        },
        "styles": {
          "description": "Style properties for the component. Common styles plus component-specific styles.",
          "allOf": [
            {
              "$ref": "#/$defs/CommonStyles"
            }
          ],
          "additionalProperties": true
        },
        "onClick": {
          "type": "array",
          "description": "Array of EventHandlers to execute sequentially on click",
          "items": {
            "$ref": "#/$defs/EventHandler"
          }
        },
        "accessibility": {
          "type": "object",
          "description": "Accessibility attributes for assistive technologies like screen readers.",
          "properties": {
            "label": {
              "type": "string",
              "description": "A short string (1-3 words) used by assistive technologies to convey the element's purpose. E.g. 'Submit', 'User ID'."
            },
            "description": {
              "type": "string",
              "description": "Additional information for assistive technologies, such as instructions or format requirements. E.g. 'Silences notifications about this conversation'."
            }
          },
          "additionalProperties": false
        }
      }
    },
    "EventHandler": {
      "type": "object",
      "description": "A wrapper around a function call in an event handler chain. Form supports one handler per event and does not support condition/as/$context.",
      "properties": {
        "call": {
          "type": "string",
          "description": "Function name referenced by this EventHandler. Predefined and custom functions are defined via catalog extendedFunctions."
        },
        "args": {
          "type": "object",
          "description": "Arguments passed to the function referenced by call. The argument schema is defined by the referenced extended function."
        }
      },
      "required": [
        "call"
      ],
      "additionalProperties": false
    },
    "FunctionCall": {
      "type": "object",
      "description": "A call object used where an extended function is invoked. Return type is defined by the referenced extended function.",
      "properties": {
        "call": {
          "type": "string",
          "description": "Function name defined in extendedFunctions"
        },
        "args": {
          "type": "object",
          "description": "Arguments passed to the referenced function"
        }
      },
      "required": [
        "call"
      ],
      "additionalProperties": false
    },
    "ExtendedFunctionDefinition": {
      "type": "object",
      "description": "Definition of a HarmonyOS extended function. Calls reference this definition by name through FunctionCall or EventHandler.call.",
      "properties": {
        "description": {
          "type": "string"
        },
        "args": {
          "description": "JSON Schema for function arguments",
          "type": "object"
        },
        "returnType": {
          "description": "Function return type. Void functions may declare const void or omit this field.",
          "type": "object"
        },
        "interactionOnly": {
          "type": "boolean",
          "description": "Whether the function can only be used in interaction contexts"
        },
        "examples": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FunctionCall"
          }
        }
      },
      "additionalProperties": false
    },
    "Expression": {
      "type": "string",
      "description": "Expression string wrapped in {{...}}. See expression_grammar.ebnf for complete syntax; this schema only checks the wrapper shape, while the validator parses the expression body.",
      "pattern": "^\\{\\{[\\s\\S]+\\}\\}$"
    },
    "PathBinding": {
      "type": "object",
      "description": "A2UI native data binding via JSON Pointer path. The resolved value type depends on the DataModel path target. Distinct from Expression syntax; use for simple declarative data binding.",
      "properties": {
        "path": {
          "type": "string",
          "description": "JSON Pointer path to the data model value (e.g. '/user/name')"
        }
      },
      "required": [
        "path"
      ],
      "additionalProperties": false
    },
    "TemplateChildren": {
      "type": "object",
      "description": "Template child generation: iterates over data array at path, instantiating componentId for each item. Default loop variables are $index and $item, customizable via indexVar/itemVar.",
      "properties": {
        "componentId": {
          "type": "string",
          "description": "Template component ID to instantiate for each array item"
        },
        "path": {
          "type": "string",
          "description": "JSON Pointer path to the data array in DataModel"
        },
        "indexVar": {
          "type": "string",
          "description": "Custom index variable name (use with $ prefix). Default: 'index'. Must start with a letter or underscore, containing only letters, digits, and underscores.",
          "default": "index",
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$"
        },
        "itemVar": {
          "type": "string",
          "description": "Custom item data variable name (use with $ prefix). Default: 'item'. Must start with a letter or underscore, containing only letters, digits, and underscores.",
          "default": "item",
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$"
        }
      },
      "required": [
        "componentId",
        "path"
      ],
      "additionalProperties": false
    },
    "CommonStyles": {
      "type": "object",
      "description": "Common styles applicable to all extended components",
      "properties": {
        "backgroundImageSizeWithStyle": {
          "description": "Background image sizing. String enum: 'cover' (fill bounds, maintain ratio), 'contain' (fit inside bounds), 'auto' (original ratio), 'fill' (stretch to fill). Default: 'auto'. Or object {width, height} with number or string values (units: fp, vp, %).",
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "cover",
                "contain",
                "auto",
                "fill"
              ],
              "default": "auto"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "type": "object",
              "properties": {
                "width": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "height": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                }
              },
              "required": [
                "width",
                "height"
              ]
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "flexShrink": {
          "description": "Flex shrink ratio when parent container space is insufficient. Higher values shrink more. Default: 1. Range: [0, 1].",
          "oneOf": [
            {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "default": 1
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "width": {
          "description": "Component width. Number (unit: vp), string with unit (e.g. '100fp', '50%'), or enum: 'matchParent' (fill parent content area), 'wrapContent' (fit content, constrained by parent), 'fixAtIdealSize' (fit content, unconstrained).",
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "height": {
          "description": "Component height. Number (unit: vp), string with unit (e.g. '100fp', '50%'), or enum: 'matchParent' (fill parent content area), 'wrapContent' (fit content, constrained by parent), 'fixAtIdealSize' (fit content, unconstrained).",
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "aspectRatio": {
          "description": "Width-to-height ratio (width/height). Common style for all Form extended components. constraintSize takes priority over aspectRatio.",
          "oneOf": [
            {
              "type": "number",
              "default": 1
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "constraintSize": {
          "description": "Size constraints limiting component dimensions during layout. Object {minWidth, maxWidth, minHeight, maxHeight}. All four required. Values: number (vp) or string with unit.",
          "type": "object",
          "properties": {
            "minWidth": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string"
                }
              ]
            },
            "maxWidth": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string"
                }
              ]
            },
            "minHeight": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string"
                }
              ]
            },
            "maxHeight": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string"
                }
              ]
            }
          },
          "required": [
            "minWidth",
            "maxWidth",
            "minHeight",
            "maxHeight"
          ]
        },
        "backgroundImage": {
          "description": "Background image path. Local resource path only, network URLs are not supported.",
          "oneOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "margin": {
          "description": "Outer spacing around the component. Number for uniform spacing (unit: vp), or object {top, right, bottom, left} for per-side control. Values: number (vp) or string with unit.",
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "top": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "right": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "bottom": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "left": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                }
              }
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "borderRadius": {
          "description": "Border corner radius. Number for uniform radius (unit: vp), or object {topLeft, topRight, bottomLeft, bottomRight} for per-corner control. Values: number (vp) or string with unit.",
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "topLeft": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "topRight": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "bottomLeft": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "bottomRight": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                }
              }
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "visibility": {
          "description": "Component visibility. Default: 'visible'. 'visible': shown normally. 'hidden': invisible but occupies layout space. 'none': invisible and occupies no space.",
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "visible",
                "hidden",
                "none"
              ],
              "default": "visible"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "clip": {
          "description": "Whether to clip child content to the component's boundary. Default: false.",
          "oneOf": [
            {
              "type": "boolean",
              "default": false
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "backgroundColor": {
          "description": "Background color in 0xARGB hex format (e.g. '#00FF0000'). Default: transparent.",
          "oneOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "borderWidth": {
          "description": "Border width. Number (unit: vp) or string with unit (e.g. '2vp').",
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "borderColor": {
          "description": "Border color in 0xARGB hex format. Default: Color.Black.",
          "oneOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "padding": {
          "description": "Inner spacing between component edge and content. Number for uniform spacing (unit: vp), or object {top, right, bottom, left} for per-side control. Values: number (vp) or string with unit.",
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "top": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "right": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "bottom": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                },
                "left": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    }
                  ]
                }
              }
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "layoutWeight": {
          "description": "Layout weight for proportional sizing within parent Row/Column. Default: 1. Only effective when parent is Row or Column.",
          "oneOf": [
            {
              "type": "number",
              "default": 1
            },
            {
              "$ref": "#/$defs/Expression"
            },
            {
              "$ref": "#/$defs/PathBinding"
            }
          ]
        },
        "shadow": {
          "description": "Shadow effect. Object {offsetX, offsetY, radius, color, fill, type}. radius is required, range [0, +inf). Default offsetX/offsetY: 0vp. Or string enum: 'outerDefaultXS', 'outerDefaultSM', 'outerDefaultMD', 'outerDefaultLG', 'outerFloatingSM', 'outerFloatingMD'.",
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "offsetX": {
                  "type": "number",
                  "default": 0
                },
                "offsetY": {
                  "type": "number",
                  "default": 0
                },
                "radius": {
                  "type": "number",
                  "minimum": 0
                },
                "color": {
                  "type": "string"
                },
                "fill": {
                  "type": "boolean",
                  "default": false
                },
                "type": {
                  "type": "string",
                  "enum": [
                    "color",
                    "blur"
                  ],
                  "default": "color"
                }
              },
              "required": [
                "radius"
              ]
            },
            {
              "type": "string",
              "enum": [
                "outerDefaultXS",
                "outerDefaultSM",
                "outerDefaultMD",
                "outerDefaultLG",
                "outerFloatingSM",
                "outerFloatingMD"
              ]
            }
          ]
        },
        "linearGradient": {
          "description": "Linear gradient fill. Object {angle, direction, colors, repeating}. angle: number, default 180. direction: enum (Left/Top/Right/Bottom/LeftTop/LeftBottom/RightTop/RightBottom/None), default None. colors: required, array of [color, position] pairs. repeating: boolean, default false.",
          "type": "object",
          "properties": {
            "angle": {
              "type": "number",
              "default": 180
            },
            "direction": {
              "type": "string",
              "enum": [
                "Left",
                "Top",
                "Right",
                "Bottom",
                "LeftTop",
                "LeftBottom",
                "RightTop",
                "RightBottom",
                "None"
              ],
              "default": "None"
            },
            "colors": {
              "type": "array",
              "description": "Array of [ResourceColor, number] pairs for gradient color stops",
              "items": {
                "type": "array",
                "items": {},
                "minItems": 2,
                "maxItems": 2
              },
              "default": []
            },
            "repeating": {
              "type": "boolean",
              "default": false
            }
          },
          "required": [
            "colors"
          ]
        }
      },
      "additionalProperties": true
    },
    "VariableScope": {
      "description": "Global variables accessible in any expression. Includes DataModel variables (surface-level scope, one instance per surface) and system variables (truly global).",
      "type": "object",
      "properties": {
        "__dataModel": {
          "description": "Data model object containing all application data for the current surface. Surface-level scope (not shared across surfaces). Access using dot notation: $__dataModel.path.to.property",
          "type": "object"
        }
      }
    },
    "LocalVariableScope": {
      "description": "Local variables available in specific contexts",
      "type": "object",
      "properties": {
        "index": {
          "description": "Current iteration index (starts from 0). Available in parent components that use template to generate child components (Row, Column, List).",
          "type": "number"
        },
        "item": {
          "description": "Current iteration item data. Type is 'any' (can be a primitive or object). Available in parent components that use template to generate child components.",
          "type": "object",
          "additionalProperties": true
        }
      }
    },
    "ExpressionOperator": {
      "description": "Operators supported in expressions, listed by precedence (highest to lowest). See expression_grammar.ebnf for complete syntax.",
      "type": "object",
      "properties": {
        "precedence": {
          "description": "Operator precedence from 1 (highest) to 10 (lowest)",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "level": {
                "type": "number"
              },
              "operators": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "symbol": {
                      "type": "string"
                    },
                    "description": {
                      "type": "string"
                    },
                    "example": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      },
      "default": {
        "precedence": [
          {
            "level": 1,
            "description": "Parentheses",
            "operators": [
              {
                "symbol": "()",
                "description": "Parentheses for grouping",
                "example": "{{ ($parentWidth - 20) / 2 }}"
              }
            ]
          },
          {
            "level": 2,
            "description": "Member access",
            "operators": [
              {
                "symbol": ".",
                "description": "Dot notation for property access",
                "example": "{{ $user.name }}"
              },
              {
                "symbol": "[]",
                "description": "Bracket notation for property/array access",
                "example": "{{ $items[0] }}"
              }
            ]
          },
          {
            "level": 3,
            "description": "Logical NOT",
            "operators": [
              {
                "symbol": "!",
                "description": "Logical negation",
                "example": "{{ !$isDisabled }}"
              }
            ]
          },
          {
            "level": 4,
            "description": "Multiplicative",
            "operators": [
              {
                "symbol": "*",
                "description": "Multiplication",
                "example": "{{ $price * $quantity }}"
              },
              {
                "symbol": "/",
                "description": "Division",
                "example": "{{ $total / $count }}"
              },
              {
                "symbol": "%",
                "description": "Modulo (remainder)",
                "example": "{{ $index % 2 }}"
              }
            ]
          },
          {
            "level": 5,
            "description": "Additive",
            "operators": [
              {
                "symbol": "+",
                "description": "Addition or string concatenation",
                "example": "{{ $basePrice + $tax }}"
              },
              {
                "symbol": "-",
                "description": "Subtraction",
                "example": "{{ $max - $min }}"
              }
            ]
          },
          {
            "level": 6,
            "description": "Relational",
            "operators": [
              {
                "symbol": ">",
                "description": "Greater than",
                "example": "{{ $age > 18 }}"
              },
              {
                "symbol": ">=",
                "description": "Greater than or equal",
                "example": "{{ $score >= 60 }}"
              },
              {
                "symbol": "<",
                "description": "Less than",
                "example": "{{ $count < 10 }}"
              },
              {
                "symbol": "<=",
                "description": "Less than or equal",
                "example": "{{ $price <= 100 }}"
              }
            ]
          },
          {
            "level": 7,
            "description": "Equality",
            "operators": [
              {
                "symbol": "==",
                "description": "Equal to",
                "example": "{{ $status == 'active' }}"
              },
              {
                "symbol": "!=",
                "description": "Not equal to",
                "example": "{{ $type != 'admin' }}"
              }
            ]
          },
          {
            "level": 8,
            "description": "Logical AND",
            "operators": [
              {
                "symbol": "&&",
                "description": "Logical conjunction",
                "example": "{{ $isLogin && !$isLocked }}"
              }
            ]
          },
          {
            "level": 9,
            "description": "Logical OR",
            "operators": [
              {
                "symbol": "||",
                "description": "Logical disjunction",
                "example": "{{ $isAdmin || $isModerator }}"
              }
            ]
          },
          {
            "level": 10,
            "description": "Ternary conditional",
            "operators": [
              {
                "symbol": "?:",
                "description": "Conditional (if-then-else)",
                "example": "{{ $age >= 18 ? 'adult' : 'minor' }}"
              }
            ]
          }
        ]
      }
    },
    "DataType": {
      "description": "Data types supported in expressions",
      "type": "object",
      "properties": {
        "string": {
          "description": "Text strings. Use single quotes for literals: 'hello world'. Use + operator for string concatenation: 'Hello, ' + $name",
          "type": "string",
          "examples": [
            "'hello'",
            "'user123'",
            "'Hello, ' + $name"
          ]
        },
        "number": {
          "description": "Numeric values (integers or floating-point)",
          "type": "number",
          "examples": [
            "42",
            "3.14",
            "-10",
            "0.5"
          ]
        },
        "boolean": {
          "description": "Boolean values. Must be lowercase: true or false",
          "type": "boolean",
          "enum": [
            true,
            false
          ],
          "examples": [
            "true",
            "false"
          ]
        }
      }
    },
    "ExpressionBuiltinFunction": {
      "description": "Builtin functions available only inside expressions {{ }}. Distinct from extendedFunctions which are EventHandler chain calls. Currently only size() is supported.",
      "type": "object",
      "properties": {
        "size": {
          "description": "Returns the length of an array. Parameter type: array. Returns 0 when the argument is not an array.",
          "type": "object",
          "properties": {
            "syntax": {
              "const": "size(arr)"
            },
            "parameterType": {
              "const": "array"
            },
            "returnType": {
              "const": "number"
            },
            "examples": [
              "{{ size($items) }}",
              "{{ size($items) + ' items' }}"
            ]
          }
        }
      }
    },
    "ExpressionTypeCoercion": {
      "description": "Automatic type coercion rules for expression operators. Type coercion failure returns empty string \"\".",
      "type": "object",
      "properties": {
        "falsyDefinition": {
          "description": "Falsy values: false, 0, '' (empty string). All other values are truthy.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "false",
            "0",
            "''"
          ]
        },
        "addition": {
          "description": "Addition operator + rules. If either operand is string, concatenate (convert the other to string); otherwise, arithmetic addition (boolean converts to number).",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string"
              },
              "rule": {
                "type": "string"
              },
              "examples": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "default": [
            {
              "operation": "string + string",
              "rule": "No conversion, string concatenation",
              "examples": [
                "'Hello, ' + $name → 'Hello, Alice'"
              ]
            },
            {
              "operation": "string + number",
              "rule": "number to string, concatenate",
              "examples": [
                "'count: ' + 3 → 'count: 3'"
              ]
            },
            {
              "operation": "string + boolean",
              "rule": "boolean to string, concatenate",
              "examples": [
                "'flag: ' + true → 'flag: true'"
              ]
            },
            {
              "operation": "number + string",
              "rule": "number to string, concatenate",
              "examples": [
                "3 + ' items' → '3 items'"
              ]
            },
            {
              "operation": "boolean + string",
              "rule": "boolean to string, concatenate",
              "examples": [
                "true + ' yes' → 'true yes'"
              ]
            },
            {
              "operation": "number + number",
              "rule": "No conversion, arithmetic addition",
              "examples": [
                "3 + 5 → 8"
              ]
            },
            {
              "operation": "boolean + number",
              "rule": "boolean to number (true=1, false=0), arithmetic addition",
              "examples": [
                "true + 2 → 3"
              ]
            },
            {
              "operation": "number + boolean",
              "rule": "boolean to number (true=1, false=0), arithmetic addition",
              "examples": [
                "5 + false → 5"
              ]
            },
            {
              "operation": "boolean + boolean",
              "rule": "Both to number, arithmetic addition",
              "examples": [
                "true + true → 2"
              ]
            }
          ]
        },
        "arithmetic": {
          "description": "Arithmetic operators - * / % rules. All operands convert to number. Boolean converts directly, string attempts conversion, failure returns \"\".",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string"
              },
              "rule": {
                "type": "string"
              },
              "examples": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "default": [
            {
              "operation": "number - number",
              "rule": "No conversion, direct calculation",
              "examples": [
                "10 - 3 → 7"
              ]
            },
            {
              "operation": "boolean - number",
              "rule": "boolean to number",
              "examples": [
                "true - 1 → 0"
              ]
            },
            {
              "operation": "number - boolean",
              "rule": "boolean to number",
              "examples": [
                "5 - false → 5"
              ]
            },
            {
              "operation": "boolean - boolean",
              "rule": "Both to number",
              "examples": [
                "true - false → 1"
              ]
            },
            {
              "operation": "string(numeric) - number",
              "rule": "string to number, then calculate",
              "examples": [
                "'10' - 3 → 7"
              ]
            },
            {
              "operation": "number - string(numeric)",
              "rule": "string to number, then calculate",
              "examples": [
                "10 - '3' → 7"
              ]
            },
            {
              "operation": "string(non-numeric) - number",
              "rule": "Conversion failure, returns \"\"",
              "examples": [
                "'abc' - 3 → \"\""
              ]
            },
            {
              "operation": "string - string",
              "rule": "Both attempt number conversion, either failure returns \"\"",
              "examples": [
                "'10' - '3' → 7",
                "'abc' - '3' → \"\""
              ]
            }
          ]
        },
        "comparison": {
          "description": "Comparison operators > >= < <= rules. Same type string: dictionary order; otherwise: convert to number, string conversion failure returns \"\".",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string"
              },
              "rule": {
                "type": "string"
              },
              "examples": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "default": [
            {
              "operation": "number > number",
              "rule": "No conversion, numeric comparison",
              "examples": [
                "10 > 3 → true"
              ]
            },
            {
              "operation": "string > string",
              "rule": "Dictionary (lexicographic) order comparison",
              "examples": [
                "'abc' > 'abb' → true"
              ]
            },
            {
              "operation": "boolean > boolean",
              "rule": "Both to number",
              "examples": [
                "true > false → true"
              ]
            },
            {
              "operation": "number > boolean",
              "rule": "boolean to number",
              "examples": [
                "2 > true → true"
              ]
            },
            {
              "operation": "boolean > number",
              "rule": "boolean to number",
              "examples": [
                "false > 0 → false"
              ]
            },
            {
              "operation": "string(numeric) > number",
              "rule": "string to number, then compare",
              "examples": [
                "'20' > 18 → true"
              ]
            },
            {
              "operation": "number > string(numeric)",
              "rule": "string to number, then compare",
              "examples": [
                "18 > '10' → true"
              ]
            },
            {
              "operation": "string(non-numeric) > number",
              "rule": "Conversion failure, returns \"\"",
              "examples": [
                "'abc' > 3 → \"\""
              ]
            }
          ]
        },
        "equality": {
          "description": "Equality operators == != rules. Same type: direct comparison; different types: boolean to number or string, string attempts number conversion.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string"
              },
              "rule": {
                "type": "string"
              },
              "examples": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "default": [
            {
              "operation": "same type == same type",
              "rule": "No conversion, direct value comparison",
              "examples": [
                "3 == 3 → true",
                "'a' == 'b' → false"
              ]
            },
            {
              "operation": "number == boolean",
              "rule": "boolean to number",
              "examples": [
                "1 == true → true",
                "0 == false → true"
              ]
            },
            {
              "operation": "string == boolean",
              "rule": "boolean to string",
              "examples": [
                "'true' == true → true",
                "'false' == false → true"
              ]
            },
            {
              "operation": "string(numeric) == number",
              "rule": "string to number, then compare",
              "examples": [
                "'42' == 42 → true"
              ]
            },
            {
              "operation": "number == string(numeric)",
              "rule": "string to number, then compare",
              "examples": [
                "42 == '42' → true"
              ]
            },
            {
              "operation": "string(non-numeric) == number",
              "rule": "Conversion failure, returns false (!= returns true)",
              "examples": [
                "'abc' == 42 → false"
              ]
            }
          ]
        },
        "logical": {
          "description": "Logical operators && || rules. Short-circuit evaluation, returns actual value (not necessarily boolean).",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string"
              },
              "rule": {
                "type": "string"
              },
              "examples": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "default": [
            {
              "operation": "truthy && value",
              "rule": "Returns value (right operand)",
              "examples": [
                "$isLogin && $userName → $userName's value"
              ]
            },
            {
              "operation": "falsy && value",
              "rule": "Returns falsy value (short-circuit)",
              "examples": [
                "false && $name → false"
              ]
            },
            {
              "operation": "truthy || value",
              "rule": "Returns truthy value (short-circuit)",
              "examples": [
                "$name || 'default' → $name's value"
              ]
            },
            {
              "operation": "falsy || value",
              "rule": "Returns value (right operand)",
              "examples": [
                "0 || 42 → 42"
              ]
            }
          ]
        },
        "logicalNotAndTernary": {
          "description": "Logical NOT ! and ternary conditional ? : rules. Uses falsy definition (false, 0, '').",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string"
              },
              "rule": {
                "type": "string"
              },
              "examples": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "default": [
            {
              "operation": "!falsy",
              "rule": "Returns true",
              "examples": [
                "!false → true",
                "!0 → true",
                "!'' → true"
              ]
            },
            {
              "operation": "!truthy",
              "rule": "Returns false",
              "examples": [
                "!true → false",
                "!1 → false",
                "!'hello' → false"
              ]
            },
            {
              "operation": "truthy ? a : b",
              "rule": "Returns a",
              "examples": [
                "$count ? $count + ' items' : 'empty'"
              ]
            },
            {
              "operation": "falsy ? a : b",
              "rule": "Returns b",
              "examples": [
                "0 ? 'yes' : 'no' → 'no'"
              ]
            }
          ]
        }
      }
    },
    "PathSyntax": {
      "description": "Path syntax for accessing data in the data model. Three path types: absolute, relative, and JSON Pointer.",
      "type": "object",
      "properties": {
        "absolutePath": {
          "description": "Absolute path starting from data model root using $__dataModel prefix. Available in any expression.",
          "syntax": "$__dataModel.path.to.property",
          "examples": [
            "{{ $__dataModel.user.name }}",
            "{{ $__dataModel.items[0].price }}"
          ],
          "notes": "Must use $__dataModel prefix. Use . for property access. Use [] for array index access only."
        },
        "relativePath": {
          "description": "Relative path from current scope using $ prefix. Only available in sub-component templates (Row, Column, List).",
          "syntax": "$propertyName",
          "examples": [
            "{{ $item.fieldName }}",
            "{{ $index }}",
            "{{ $items[$index] }}"
          ],
          "notes": "Uses $ prefix. In sub-component templates, $item refers to current iteration item and $index to current iteration index.."
        },
        "jsonPointer": {
          "description": "JSON Pointer syntax using ${/path/to/value}. Equivalent to $__dataModel prefix but uses / separator instead of dot notation. Available in any expression.",
          "syntax": "${/json/pointer/path}",
          "examples": [
            "{{ ${/user/profile/name} }}",
            "{{ ${/items/0/selected} }}"
          ],
          "notes": "Equivalent to absolute path: ${/user/name} ≡ $__dataModel.user.name. Uses / as separator. Distinct from A2UI native 'path' attribute which uses JSON Pointer as literal values without expression support."
        },
        "arrayAccess": {
          "description": "Array element access using bracket notation",
          "syntax": "$arrayName[index] or $arrayName[$indexVariable]",
          "examples": [
            "{{ $items[0] }}",
            "{{ $items[$index] }}",
            "{{ $__dataModel.users[$userId].name }}"
          ],
          "notes": "Index can be a number constant or a variable. Supports chain access like $items[0].user.name"
        }
      }
    },
    "ExpressionReactivity": {
      "description": "Reactive expressions automatically re-evaluate when referenced variables change. Only DataModel variables ($__dataModel.xxx) are reactive. Template local variables ($item, $index, or custom itemVar/indexVar variables) are scoped to template expansion.",
      "type": "object",
      "properties": {
        "behavior": {
          "description": "When a reactive variable used in an expression changes, the expression automatically re-evaluates and updates the associated property or style",
          "type": "string"
        },
        "updateScope": {
          "description": "Reactive updates apply to component properties (content, text, condition, visible, etc.) and component style properties (fontSize, fontColor, width, etc.)",
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "component properties",
              "style properties"
            ]
          }
        },
        "evaluationFailure": {
          "description": "When expression evaluation fails, returns empty string \"\" without interrupting rendering",
          "type": "string"
        },
        "reservedCharacter": {
          "description": "# character is reserved for future use to disable reactivity on specific expressions",
          "type": "string"
        }
      },
      "examples": [
        {
          "description": "Dynamic style based on data model",
          "expression": "{{ $__dataModel.theme.primaryColor }}",
          "triggers": [
            "$__dataModel.theme.primaryColor change"
          ]
        },
        {
          "description": "Conditional visibility based on array emptiness",
          "expression": "{{ size($items) > 0 }}",
          "triggers": [
            "$items array change"
          ]
        }
      ]
    }
  },
  "components": {
    "Text": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "properties": {
            "component": {
              "const": "Text"
            },
            "content": {
              "description": "Text content to display. Supports expression binding for dynamic content.",
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/$defs/Expression"
                },
                {
                  "$ref": "#/$defs/PathBinding"
                }
              ]
            }
          },
          "required": [
            "component",
            "content"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "textOverflow": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "clip",
                        "ellipsis"
                      ],
                      "default": "clip"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Text overflow behavior when content exceeds maxLines. Default: 'clip'. 'clip': truncate at max lines. 'ellipsis': show ellipsis for overflow. Must be used with maxLines."
                },
                "fontSize": {
                  "oneOf": [
                    {
                      "type": "number",
                      "default": 16
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Font size in fp. Default: 16fp."
                },
                "fontWeight": {
                  "oneOf": [
                    {
                      "type": "number",
                      "default": 400,
                      "minimum": 100
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Font weight. Default: 400. Range: [100, 900] in steps of 100."
                },
                "fontColor": {
                  "oneOf": [
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Font color in hex format (e.g. '#333333')."
                },
                "textAlign": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "start",
                        "center",
                        "end",
                        "justify"
                      ],
                      "default": "start"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Horizontal text alignment. Default: 'start'. Options: start (align leading), center, end (align trailing), justify (both edges)."
                },
                "maxLines": {
                  "oneOf": [
                    {
                      "type": "number",
                      "minimum": 0
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Maximum number of text lines. Default: inf (no limit when unset or invalid). Range: [0, inf]. Use with textOverflow for overflow behavior."
                },
                "maxFontSize": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Maximum adaptive font size in fp. Must be used with minFontSize and maxLines or layout constraints. No effect when maxFontSize <= 0 or maxFontSize < minFontSize."
                },
                "minFontSize": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Minimum adaptive font size in fp. Must be used with maxFontSize and maxLines or layout constraints. No effect when minFontSize <= 0."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Display component for rendering text content. Supports font styling, color, text overflow, and adaptive font sizing."
        }
      ],
      "unevaluatedProperties": false
    },
    "Button": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "properties": {
            "component": {
              "const": "Button"
            },
            "label": {
              "description": "Text label displayed on the button. Supports expression binding.",
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/$defs/Expression"
                },
                {
                  "$ref": "#/$defs/PathBinding"
                }
              ]
            },
            "enabled": {
              "description": "Whether the button is interactive. Default: true. Disabled buttons are visually distinct and non-clickable.",
              "oneOf": [
                {
                  "type": "boolean"
                },
                {
                  "$ref": "#/$defs/Expression"
                },
                {
                  "$ref": "#/$defs/PathBinding"
                }
              ],
              "default": true
            }
          },
          "required": [
            "component",
            "label"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "fontColor": {
                  "oneOf": [
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Button text color in hex format."
                },
                "fontSize": {
                  "oneOf": [
                    {
                      "type": "number",
                      "default": 16
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Font size in fp. Default: 16fp."
                },
                "fontWeight": {
                  "oneOf": [
                    {
                      "type": "number",
                      "default": 500
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Font weight. Default: 500. Number [100-900] in steps of 100, or enum: 'lighter'(100), 'normal'(400), 'regular'(400), 'medium'(500), 'bold'(700), 'bolder'(900)."
                },
                "maxFontSize": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Maximum adaptive font size in fp. See Text.maxFontSize for usage rules."
                },
                "minFontSize": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Minimum adaptive font size in fp. Must be used with maxFontSize or layout constraints. No effect when minFontSize <= 0."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Interactive component for user actions. Supports text label, enabled/disabled state. Responds to onClick events."
        }
      ],
      "unevaluatedProperties": false
    },
    "Row": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Horizontal layout component that arranges children in a row",
          "properties": {
            "component": {
              "const": "Row"
            },
            "children": {
              "description": "Child component ID list or template component ID and loop data path.",
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "$ref": "#/$defs/TemplateChildren"
                }
              ]
            },
            "itemMargin": {
              "description": "Horizontal spacing between children in vp. Default: 16.",
              "type": "number",
              "default": 16
            }
          },
          "required": [
            "component"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "justifyContent": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "start",
                        "center",
                        "end",
                        "spaceBetween",
                        "spaceAround",
                        "spaceEvenly"
                      ],
                      "default": "start"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Horizontal alignment of children along main axis. Default: 'start'. Options: start, center, end, spaceBetween (edges flush, equal gaps), spaceAround (half-gap at edges), spaceEvenly (equal gaps everywhere)."
                },
                "alignItems": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "top",
                        "center",
                        "bottom"
                      ],
                      "default": "center"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Vertical alignment of children along cross axis. Default: 'center'. 'top': align to top. 'center': vertically centered. 'bottom': align to bottom."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Horizontal layout component that arranges children in a row. Supports template-based child generation and configurable item spacing (itemMargin)."
        }
      ],
      "unevaluatedProperties": false
    },
    "Column": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Vertical layout component that arranges children in a column",
          "properties": {
            "component": {
              "const": "Column"
            },
            "children": {
              "description": "Child component ID list or template component ID and loop data path.",
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "$ref": "#/$defs/TemplateChildren"
                }
              ]
            },
            "itemMargin": {
              "description": "Vertical spacing between children in vp. Default: 8.",
              "type": "number",
              "default": 8
            }
          },
          "required": [
            "component"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "justifyContent": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "start",
                        "center",
                        "end",
                        "spaceBetween",
                        "spaceAround",
                        "spaceEvenly"
                      ],
                      "default": "start"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Vertical alignment of children along main axis. Default: 'start'. Options: start, center, end, spaceBetween (edges flush, equal gaps), spaceAround (half-gap at edges), spaceEvenly (equal gaps everywhere)."
                },
                "alignItems": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "start",
                        "center",
                        "end"
                      ],
                      "default": "start"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Horizontal alignment of children along cross axis. Default: 'start'. 'start': align to leading edge. 'center': horizontally centered. 'end': align to trailing edge."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Vertical layout component that arranges children in a column. Supports template-based child generation and configurable item spacing (itemMargin)."
        }
      ],
      "unevaluatedProperties": false
    },
    "List": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Scrollable list component for efficiently displaying many items",
          "properties": {
            "component": {
              "const": "List"
            },
            "children": {
              "description": "Child component ID list or template component ID and loop data path.",
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "$ref": "#/$defs/TemplateChildren"
                }
              ]
            },
            "space": {
              "description": "Spacing between list items in vp. Default: 0. Negative values or values >= content area length are treated as default.",
              "type": "number",
              "default": 0
            }
          },
          "required": [
            "component"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "listDirection": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "vertical",
                        "horizontal"
                      ],
                      "default": "vertical"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "List arrangement direction. Default: 'vertical'. 'vertical': items stacked vertically. 'horizontal': items arranged horizontally."
                },
                "scrollBar": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "off",
                        "auto",
                        "on"
                      ],
                      "default": "auto"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Scroll bar visibility. Default: 'auto'. 'off': never show. 'auto': show on touch, hide after 2s. 'on': always visible."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Scrollable list component for efficiently displaying many items. Supports template-based child generation, horizontal/vertical direction, and scroll bar control."
        }
      ],
      "unevaluatedProperties": false
    },
    "Stack": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Stack layout where children are stacked on top of each other",
          "properties": {
            "component": {
              "const": "Stack"
            },
            "children": {
              "description": "Array of child component IDs stacked on top of each other.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "component"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "alignContent": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "topStart",
                        "top",
                        "topEnd",
                        "start",
                        "center",
                        "end",
                        "bottomStart",
                        "bottom",
                        "bottomEnd"
                      ],
                      "default": "center"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Alignment of children within the stack. Default: 'center'. Options: topStart, top, topEnd, start, center, end, bottomStart, bottom, bottomEnd."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Stack layout component where children are layered on top of each other. Supports 9-position alignment."
        }
      ],
      "unevaluatedProperties": false
    },
    "Image": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Image component for displaying local, network, or resource images",
          "properties": {
            "component": {
              "const": "Image"
            },
            "src": {
              "description": "Image source: local or resource path only. Network URLs are not supported. Resource-path SVG is supported; base64 inline SVG data URI is not supported. Supports expression/path binding for dynamic local resource path.",
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/$defs/Expression"
                },
                {
                  "$ref": "#/$defs/PathBinding"
                }
              ]
            }
          },
          "required": [
            "component",
            "src"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "aspectRatio": {
                  "oneOf": [
                    {
                      "type": "number",
                      "default": 1
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Width-to-height ratio (width/height). Default: 1.0. When only width is set, height = width/ratio. When only height is set, width = height*ratio. When both set with ratio, height is recalculated. constraintSize takes priority over aspectRatio."
                },
                "objectFit": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "fill",
                        "contain",
                        "cover",
                        "auto",
                        "none",
                        "scaleDown",
                        "topStart",
                        "top",
                        "topEnd",
                        "start",
                        "center",
                        "end",
                        "bottomStart",
                        "bottom",
                        "bottomEnd",
                        "matrix"
                      ],
                      "default": "cover"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Image fill mode. Default: 'cover'. 'fill': stretch to fill. 'contain': fit inside maintaining ratio. 'cover': fill covering bounds. 'auto': proportional scaling. 'none': original size centered. 'scaleDown': shrink or keep original. Also supports alignment positions: topStart/top/topEnd/start/center/end/bottomStart/bottom/bottomEnd, and 'matrix' for custom positioning."
                },
                "fillColor": {
                  "oneOf": [
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Image tint color. ResourceColor in #AARRGGBB or #RRGGBB form. Effective for SVG fill color; PNG and other bitmaps parse normally but may not be visually tinted."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Image component for displaying local or resource images. Supports resource-path SVG, aspect ratio, fillColor, and object-fit modes."
        }
      ],
      "unevaluatedProperties": false
    },
    "Divider": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Divider component for visually separating content areas",
          "properties": {
            "component": {
              "const": "Divider"
            }
          },
          "required": [
            "component"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "strokeWidth": {
                  "oneOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "default": "1px",
                  "description": "Divider line width. Default: '1px'. Number (unit: vp) or string with unit. Invalid values use default."
                },
                "vertical": {
                  "oneOf": [
                    {
                      "type": "boolean",
                      "default": false
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Divider direction. Default: false (horizontal). true for vertical divider."
                },
                "color": {
                  "oneOf": [
                    {
                      "type": "string",
                      "default": "#33000000"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Divider color in hex format. Default: light mode #33000000, dark mode #33FFFFFF."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Divider component for visually separating content areas. Supports horizontal/vertical orientation, custom width, and color."
        }
      ],
      "unevaluatedProperties": false
    },
    "Progress": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Progress bar component for displaying task completion progress",
          "properties": {
            "component": {
              "const": "Progress"
            },
            "value": {
              "description": "Current progress value. Required. Valid range is [0, total] when total is present.",
              "type": "number",
              "default": 0
            },
            "total": {
              "description": "Total progress value representing 100% completion. Optional.",
              "type": "number"
            }
          },
          "required": [
            "component",
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "color": {
                  "oneOf": [
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Progress bar foreground color in hex format. LinearGradient is not supported."
                },
                "type": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "linear",
                        "ring",
                        "eclipse",
                        "scaleRing",
                        "capsule"
                      ],
                      "default": "linear"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Progress bar style. Default: 'linear'. 'linear': horizontal bar. 'ring': circular ring without ticks. 'eclipse': moon-phase circular. 'scaleRing': clock-tick circular. 'capsule': capsule shape (vertical when height > width)."
                },
                "strokeWidth": {
                  "oneOf": [
                    {
                      "type": "number",
                      "default": 4
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Progress stroke width in vp. Default: 4.0vp. Effective only when type is linear, ring, or scaleRing; not effective for eclipse or capsule."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Progress bar component for displaying task completion. Supports linear, ring, eclipse, scaleRing, and capsule styles."
        }
      ],
      "unevaluatedProperties": false
    },
    "Checkbox": {
      "type": "object",
      "allOf": [
        {
          "$ref": "#/$defs/ExtendedComponentCommon"
        },
        {
          "type": "object",
          "description": "Checkbox component for multiple selection",
          "properties": {
            "component": {
              "const": "Checkbox"
            },
            "label": {
              "description": "Text label displayed next to the checkbox.",
              "type": "string"
            },
            "value": {
              "description": "Semantic identifier for the checkbox. Not rendered visually.",
              "type": "string"
            },
            "select": {
              "description": "Whether the checkbox is initially selected.",
              "type": "boolean"
            }
          },
          "required": [
            "component"
          ]
        },
        {
          "type": "object",
          "properties": {
            "styles": {
              "type": "object",
              "properties": {
                "selectedColor": {
                  "oneOf": [
                    {
                      "type": "string"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Checkbox color when selected, in hex format."
                },
                "shape": {
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": [
                        "circle",
                        "rounded_square"
                      ],
                      "default": "circle"
                    },
                    {
                      "$ref": "#/$defs/Expression"
                    },
                    {
                      "$ref": "#/$defs/PathBinding"
                    }
                  ],
                  "description": "Checkbox shape. Default: 'circle'. 'circle': circular shape. 'rounded_square': rounded square shape."
                }
              },
              "additionalProperties": true
            }
          },
          "description": "Checkbox component for multiple selection. Supports custom shape (circle/rounded_square) and mark styling. Can be used standalone or grouped via the 'group' property."
        }
      ],
      "unevaluatedProperties": false
    }
  },
  "extendedFunctions": {},
  "variableSystem": {
    "global": {
      "$ref": "#/$defs/VariableScope"
    },
    "local": {
      "$ref": "#/$defs/LocalVariableScope"
    }
  },
  "expressionOperators": {
    "$ref": "#/$defs/ExpressionOperator"
  },
  "dataTypes": {
    "$ref": "#/$defs/DataType"
  },
  "expressionBuiltinFunctions": {
    "$ref": "#/$defs/ExpressionBuiltinFunction"
  },
  "expressionTypeCoercion": {
    "$ref": "#/$defs/ExpressionTypeCoercion"
  },
  "pathSyntax": {
    "$ref": "#/$defs/PathSyntax"
  },
  "expressionReactivity": {
    "$ref": "#/$defs/ExpressionReactivity"
  },
  "expressionUsageConstraints": {
    "description": "Constraints and limitations for expression usage in extended components",
    "type": "object",
    "properties": {
      "supportedComponents": {
        "description": "Only extended components (marked with *) support expressions",
        "type": "string"
      },
      "supportedMessages": {
        "description": "Expressions only work in updateComponents messages",
        "type": "string"
      },
      "restrictedProperties": {
        "description": "Cannot use expressions in 'component' or 'id' properties",
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "pathRestriction": {
        "description": "A2UI native data binding 'path' attribute does not support expressions (literal JSON Pointer only). However, function argument 'path' in custom functions can support expressions.",
        "type": "string"
      },
      "handlerRestriction": {
        "description": "EventHandler 'call' and 'as' properties cannot use expressions",
        "type": "string"
      },
      "valueOnly": {
        "description": "Expressions can only be used in values, not in keys/property names",
        "type": "string"
      },
      "sizeLimit": {
        "description": "Expression string total length must not exceed 2048 characters",
        "type": "number"
      },
      "nestingLimit": {
        "description": "Parenthesis nesting depth must not exceed 20 levels",
        "type": "number"
      },
      "schemaAuthority": {
        "description": "Whether each property supports expressions is ultimately determined by the catalog JSON Schema definition (Expression union type). If a property's schema includes Expression as an option, it supports expressions; otherwise it does not.",
        "type": "string"
      }
    }
  }
}
